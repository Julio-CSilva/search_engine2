[project]
name = "searchboldsystems-coi"
version = "0.1.0"
description = "Check COI marker availability per species in BOLD Systems."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.34.2",
    "python-dotenv>=1.2.2",
]
